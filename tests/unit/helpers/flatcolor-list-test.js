import { flatcolorList } from 'some-ember/helpers/flatcolor-list';
import { module, test } from 'qunit';

module('Unit | Helper | flatcolor list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = flatcolorList([42]);
  assert.ok(result);
});
